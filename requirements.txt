# Web API (FastAPI + ASGI server)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database ORM (SQLite; includes SQLAlchemy + Pydantic)
sqlmodel>=0.0.21

# PDF / macro extraction pipeline
openai>=1.40.0
python-dotenv>=1.0.0
